# Copy to .env and fill in. .env is gitignored; this file is not.
#
# Both come from the TinaCloud dashboard for the project, under the project's
# Overview / Configuration. The client id is public and already committed in
# tina/config.ts's default; the token is a secret and must never be committed.
NEXT_PUBLIC_TINA_CLIENT_ID=beec4181-dd4a-41d1-a89f-6257a916f9b8
TINA_TOKEN=
